import React from 'react'
import styled from 'styled-components'
import Paginator from '../../components/Pagination'

const styledPaginator = styled(Paginator)`
    width: 100%;
    display: flex;
    flex-direction: row;
    justify-content: center;
`

export { styledPaginator as Paginator }
